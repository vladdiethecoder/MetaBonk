"""Placeholder package for environment modules.

Some validation scripts expect a `src/environment/` path. The project currently
uses `src/env/` for environment wrappers; this package remains intentionally
empty to preserve backward compatibility with those scripts.
"""

