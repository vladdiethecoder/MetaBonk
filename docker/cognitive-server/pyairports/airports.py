"""Minimal airport dataset stub for `outlines.types.airports`.

SGLang's constrained decoding stack imports `outlines.types.airports`, which
expects `AIRPORT_LIST` to exist. The concrete contents are irrelevant for
MetaBonk, so we provide an empty list.
"""

AIRPORT_LIST: list[dict] = []

