# Optional dependencies for headless EGL/OpenGL validation and future zero-copy pipelines.
#
# These are not required for the default MetaBonk PipeWire/NVENC streaming path.
#
# EGL/OpenGL (headless pbuffer renderer)
PyOpenGL>=3.1.6
PyOpenGL_accelerate>=3.1.6
#
# CUDA bindings (already present in some environments)
cuda-python>=12.0
#
# VPF/PyNvVideoCodec (NVENC bindings) - package name varies by distribution.
# Install from NVIDIA's Video Codec SDK / VPF wheels appropriate to your CUDA version.
# PyNvVideoCodec
#
# CuPy for convenient GPU array interop (optional)
# cupy-cuda12x

