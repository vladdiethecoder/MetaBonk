[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    benchmark: performance/latency benchmarks (opt-in)
    slow: long-running tests (opt-in)
    e2e: end-to-end tests requiring external services (opt-in)
